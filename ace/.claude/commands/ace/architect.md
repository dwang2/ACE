Use the **architect** agent to handle this task: $ARGUMENTS

The architect owns system-level technical design. Scope includes: ADRs, design docs, API contracts, data models, threat models, architectural PR reviews, tradeoff analysis, dependency evaluation, and semver/public-API decisions.

Invoke the architect agent now with the task above.
