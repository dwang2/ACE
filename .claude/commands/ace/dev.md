Use the **developer** agent to handle this task: $ARGUMENTS

The developer owns code. Scope includes: implementing GitHub Issues, writing/refactoring functions and modules, adding or fixing tests, reproducing and fixing bugs, resolving failing CI, writing migration scripts, and code review on PRs.

Invoke the developer agent now with the task above.
